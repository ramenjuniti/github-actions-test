name: workflow dispatch

on:
  workflow_dispatch:

jobs:
  workflow-dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: echo
        run: echo "workflow dispatch"