name: workflow run 3-2

on:
  workflow_run:
    workflows:
      - workflow run 2
    branches:
      - develop
    types:
      - completed


jobs:
  workflow-run-3-2:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: echo
        run: echo "workflow run 3-2"